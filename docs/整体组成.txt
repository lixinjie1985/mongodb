一个mongodb集群又叫做分片集群
一个集群由三部分组成，多个分片，路由器，配置服务器
一个副本集合由多个节点组成，其中一个是主节点，其余是从节点，主节点提供读写请求，从节点只提供读请求，数据写入主节点后再往从节点同步
每个分片存储集群全部数据的一部分，分片部署为副本集合
路由器通常部署在应用程序所在的节点上，是应用和多个分片间的接口，通过访问路由器得知该向哪些分区发起请求
配置服务器存储集群的元数据和配置，部署为副本集合
多个分片把自身信息发给配置服务器，路由器从配置服务器读取多个分片信息，客户端应用先通过路由器获取相关分片信息，再向这些分片发起请求